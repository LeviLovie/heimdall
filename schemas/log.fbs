namespace log;

table Var {
  key: string;
  val: string;
}

table Context {
  app: string;
  pid: uint;
  os: string;
  version: string;
}

table Log {
  ts: string;
  msg: string;
  context: Context;
  vars: [Var];
}
