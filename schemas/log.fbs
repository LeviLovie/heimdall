namespace log;

table Var {
  key: string;
  val: string;
}

table Context {
  app: string;
  pid: uint;
  machine: string;
  os: string;
  version: string;
}

table Log {
  msg: string;
  ts: string;
  context: Context;
  vars: [Var];
}
